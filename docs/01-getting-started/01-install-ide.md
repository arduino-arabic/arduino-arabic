---
sidebar_position: 1
title: 📥 تثبيت بيئة التطوير (IDE)
description: شرح كيفية تحميل وتثبيت برنامج Arduino IDE وشرح واجهة الاستخدام.
---

# تثبيت بيئة تطوير الأردوينو (Arduino IDE)

لكي تتحدث مع لوحة الأردوينو وتعطيها الأوامر، تحتاج إلى برنامج وسيط يترجم الكود الذي تكتبه (C++) إلى لغة الآلة التي يفهمها المعالج. هذا البرنامج يسمى **Arduino IDE**.

---

## 1. تحميل البرنامج

الخطوة الأولى هي تحميل النسخة الأحدث من الموقع الرسمي. ننصح دائماً باستخدام **Arduino IDE 2.0** (النسخة الحديثة) لأنها توفر ميزات قوية مثل الإكمال التلقائي للكود وتصحيح الأخطاء.



1.  اذهب إلى [صفحة التحميل الرسمية](https://www.arduino.cc/en/software).
2.  اختر نظام التشغيل الخاص بك (Windows, macOS, أو Linux).
3.  اضغط على **"Just Download"** (إلا إذا كنت تود التبرع).
4.  بعد التحميل، اتبع خطوات التثبيت التقليدية (Next > I Agree > Install).

:::tip ملاحظة لمستخدمي Windows
تأكد من الموافقة على تثبيت **التعريفات (Drivers)** التي ستظهر لك أثناء التثبيت، فهي ضرورية لكي يتعرف الكمبيوتر على لوحة الأردوينو عند توصيلها.
:::

---

## 2. جولة في واجهة البرنامج

بعد تشغيل البرنامج لأول مرة، ستظهر لك واجهة بسيطة وجميلة. دعنا نتعرف على أهم الأزرار التي ستستخدمها يومياً:



### شريط الأدوات العلوي (Toolbar)

يحتوي الشريط العلوي على أهم زرين في عالم الأردوينو:

* **✅ التحقق (Verify):** الزر الذي يشبه علامة "الصح". وظيفته التأكد من أن الكود الذي كتبته سليم وليس به أخطاء برمجية (Syntax Errors)، لكنه **لا يرسل** الكود للوحة.
* **➡️ الرفع (Upload):** الزر الذي يشبه السهم لليمين. وظيفته التحقق من الكود أولاً، ثم **إرساله وتخزينه** داخل لوحة الأردوينو ليبدأ العمل فوراً.

### القوائم المنسدلة

* **Select Board:** في النسخة الجديدة، ستجد قائمة منسدلة في الأعلى لاختيار اللوحة والمنفذ (COM Port) الذي وصلت الأردوينو به.

---

## 3. هيكل الكود الأساسي (Sketch)

عند فتح ملف جديد، ستجد دائماً دالتين أساسيتين لا يمكن للكود أن يعمل بدونهما:

```cpp title="البنية الأساسية للكود"
void setup() {
  // ضع كود الإعداد هنا، ليعمل مرة واحدة فقط
  // مثل: تهيئة المنافذ، تشغيل الشاشة، إعداد سرعة الاتصال
}

void loop() {
  // ضع الكود الرئيسي هنا، ليعمل بشكل متكرر (إلى الأبد)
  // مثل: قراءة الحساسات، تشغيل الأضواء
}