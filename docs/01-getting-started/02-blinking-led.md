---
sidebar_position: 2
title: "💡 مشروعك الأول: الوميض (Blink)"
description: "تعلم كيف تتحكم في لوحة الأردوينو واجعل الضوء يرمش بأوامرك البرمجية."
---

# 💡 مشروعك الأول: الوميض (Blink)

أهلاً بك أيها المبدع الصغير! 🌟

في عالم البرمجة، أول شيء يتعلمه المبرمج هو طباعة جملة "Hello World". أما في عالم الإلكترونيات والروبوت، فإن أول إنجاز نحتفل به هو جعل ضوء LED يضيء وينطفئ (يرمش).

في هذا الدرس، ستصبح **"قائداً"** يصدر الأوامر للوحة الأردوينو، وستراها تنفذ أوامرك بالحرف الواحد!

---

## 🛠️ الأدوات التي نحتاجها

الخبر الجيد هو أنك **لن تحتاج لشراء أي أدوات إضافية** لهذا الدرس! سنستخدم ما هو موجود في اللوحة بالفعل.

1.  **لوحة Arduino Uno** (العقل المدبر).
2.  **كابل USB** (حلقة الوصل بين الكمبيوتر واللوحة).

### 🧐 أين الضوء المخفي؟
لوحة الأردوينو ذكية، فهي تحتوي على ضوء صغير مدمج بداخلها (Built-in LED) متصل بالمنفذ رقم **13**. سنستخدم هذا الضوء اليوم بدلاً من توصيل أسلاك خارجية.



:::info معلومة ذكية
ابحث في لوحتك عن حرف **"L"** صغير بجانب المنافذ الرقمية (قرب الرقم 13). هذا هو الضوء الذي سنتحكم به اليوم!
:::

---

## 💻 لنكتب الكود (The Sketch)

الآن، افتح برنامج **Arduino IDE**. ستجد دالتين فارغتين (`setup` و `loop`).
**امسح كل شيء في الصفحة**، واكتب الكود التالي بنفسك (أو انسخه)، وحاول قراءة التعليقات لتفهم ماذا يحدث:

```cpp title="Blink.ino"
/*
  مشروع الوميض (Blink)
  الهدف: تشغيل الضوء لمدة ثانية، ثم إطفاؤه لمدة ثانية، وتكرار ذلك للأبد.
*/

// 1. دالة الإعداد (setup): تعمل مرة واحدة فقط عند تشغيل اللوحة
void setup() {
  // نخبر الأردوينو: "يا أردوينو، المنفذ رقم 13 سنستخدمه للإخراج (OUTPUT)"
  // يعني أننا سنخرج منه كهرباء لتشغيل الضوء
  pinMode(13, OUTPUT);
}

// 2. دالة التكرار (loop): تعمل بشكل مستمر ولا تتوقف أبداً
void loop() {
  
  digitalWrite(13, HIGH);  // أمر تشغيل: أرسل كهرباء (5 فولت) للضوء
  delay(1000);             // أمر انتظار: توقف مكانك لمدة 1000 مللي ثانية (ثانية واحدة)
  
  digitalWrite(13, LOW);   // أمر إطفاء: اقطع الكهرباء (0 فولت)
  delay(1000);             // أمر انتظار: توقف مكانك لمدة ثانية واحدة
  
} // وهنا يعود الأردوينو لأول السطر في دالة loop ويكرر العملية