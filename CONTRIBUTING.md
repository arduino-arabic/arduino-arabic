# 🤝 دليل المساهمة في منصة أردوينو بالعربي

نحن سعداء جداً باهتمامك بالمساهمة في إثراء المحتوى التقني العربي! هذا الدليل يوضح الإرشادات والخطوات اللازمة لضمان أن تكون مساهمتك سلسة ومقبولة.

---

## 🧭 1. إرشادات عامة قبل البدء

1.  **اللغة:** يجب أن يكون المحتوى باللغة العربية الفصحى الواضحة والسهلة.
2.  **التنسيق:** اتبع تنسيق Markdown/MDX الموحد في المشروع.
3.  **قبل الكتابة:** يفضل دائماً فتح **[مشكلة جديدة (Issue)]** أولاً، خاصة للميزات الكبيرة أو الدروس الجديدة، لمناقشة الفكرة والتأكد من عدم وجود عمل مكرر.
4.  **الاحترام:** يجب الالتزام بـ **[ميثاق السلوك (Code of Conduct)]** لضمان بيئة مجتمعية إيجابية.

---

## ⚙️ 2. أنواع المساهمات

يمكنك المساهمة في المجالات التالية:

### أ. التوثيق والمحتوى (Docs Content)

* **إضافة درس جديد:** يجب أن يكون الدرس فريداً وغير موجود، ويتبع المسار التعليمي المنطقي.
* **تحديث درس قائم:** تصحيح معلومات غير دقيقة أو إضافة أمثلة جديدة.
* **تصحيح الأخطاء:** تصحيح أخطاء إملائية، نحوية، أو تقنية في أي صفحة.

### ب. الأكواد والأمثلة (Code Examples)

* **تحسين الأكواد:** يجب أن تكون الأكواد نظيفة، مُعلَّقة (Commented)، ومختبرة قبل الإرسال.
* **أمثلة الـ MDX:** إنشاء مكونات React تفاعلية (Interactive Components) لتحسين تجربة الدرس.

### ج. الواجهة والمظهر (UI/UX)

* تحسين تنسيقات CSS (ملف `src/css/custom.css`).
* تحسين مكونات React المخصصة.

---

## 🛠️ 3. خطوات العمل (Workflow)

هذه هي الخطوات التقنية الأساسية لإرسال مساهمتك:

1.  **الاستنساخ (Fork):** قم بـ "Fork" لهذا المستودع إلى حسابك الشخصي على GitHub.
2.  **النسخ المحلي (Clone):** انسخ المستودع إلى جهازك المحلي:
    ```bash
    git clone [https://github.com/YourUsername/arduino-arabic.git](https://github.com/YourUsername/arduino-arabic.git)
    ```
3.  **تثبيت التبعيات:**
    ```bash
    npm install  # أو yarn
    ```
4.  **إنشاء فرع (Branch):** لكل مساهمة، يرجى إنشاء فرع جديد باسم وصفي بالإنجليزية (مثلاً: `fix/button-color` أو `feat/new-sensor-lesson`).
    ```bash
    git checkout -b your-branch-name
    ```
5.  **التعديل:** قم بإجراء تعديلاتك (في ملفات `docs/` أو `blog/` أو `src/`).
6.  **التشغيل المحلي والاختبار:** تأكد أن التعديلات تعمل بشكل صحيح محلياً قبل الرفع.
    ```bash
    npm run start
    ```
7.  **الالتزام (Commit):** أضف التعديلات والتزم بها برسالة واضحة. يفضل استخدام تنسيق Commit Message موحد (مثل: `fix: تصحيح خطأ إملائي في درس الحساس الفلاني`).
    ```bash
    git add .
    git commit -m "feat: إضافة درس جديد عن حساس الموجات فوق الصوتية"
    ```
8.  **الرفع (Push):** ارفع الفرع إلى حسابك على GitHub.
    ```bash
    git push origin your-branch-name
    ```
9.  **طلب السحب (Pull Request):** افتح "Pull Request" من فرعك إلى الفرع الرئيسي (`main`) للمستودع.

---

## 📝 4. إرشادات تنسيق المحتوى (Markdown Guidelines)

عند كتابة الدروس، يرجى الالتزام بالهيكل التالي:

1.  **العنوان الرئيسي:** يجب أن يكون عنوان الصفحة (`#`).
    ```markdown
    # مقدمة في الأردوينو
    ```
2.  **العناوين الفرعية:** استخدم عناوين المستوى الثاني أو الثالث (`##` و `###`) للتنظيم.
    ```markdown
    ## 2.1 المكونات الأساسية للوحة
    ### 2.1.1 المتحكم الدقيق
    ```
3.  **صناديق الكود (Code Blocks):** استخدم علامات الاقتباس الثلاثة مع تحديد لغة البرمجة لضمان تمييز بناء الجملة (Syntax Highlighting).
    ```markdown
    ```cpp title="كود تشغيل الـ LED"
    void setup() {
      pinMode(LED_BUILTIN, OUTPUT);
    }
    // ...
    ```
4.  **الملاحظات والتنبيهات:** استخدم ميزة الـ Admonitions الخاصة بـ Docusaurus للتمييز.
    ```markdown
    :::info
    تأكد من توصيل المقاومة لتجنب إتلاف الـ LED.
    :::

    :::caution
    لا تقم بتوصيل مصدر طاقة أكبر من 12V.
    :::
    ```

نحن نتطلع لرؤية مساهماتك! شكراً لك على دعم هذا المشروع.